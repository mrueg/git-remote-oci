git-remote-oci
*.test

# Build and release artifacts
dist/

# Test and profiling output
coverage.out
coverage.html
*.cover
*.prof
*.pprof

# Editor / OS
.DS_Store
.idea/
.vscode/

# Raw coverage data, merged into coverage.out by `make cover`.
.coverdata/

# Scratch copies `make tidy-check` takes and normally removes; left behind if
# it is interrupted.
go.mod.tidycheck
go.sum.tidycheck

# benchmark.yml tees the suite's output here.
bench.log
